export const funcionesPuntos = [
  (option, setRav, setSly, setGri, setHuf, lock, setLock) => {
    if (lock === false) {
      if (option === 1) {
        setRav((Rav) => Rav + 1);
        setLock(true);
      } else if (option === 2) {
        setSly((Sly) => Sly + 2);
        setLock(true);
      } else if (option === 3) {
        setGri((Gri) => Gri + 2);
        setLock(true);
      } else if (option === 4) {
        setHuf((Huf) => Huf + 2);
        setRav((Rav) => Rav + 1);
        setLock(true);
      }
    }
  },
  (option, setRav, setSly, setGri, setHuf, lock, setLock) => {
    if (lock === false) {
      if (option === 1) {
        setRav((Rav) => Rav + 1);
        setLock(true);
      } else if (option === 2) {
        setSly((Sly) => Sly + 2);
        setLock(true);
      } else if (option === 3) {
        setGri((Gri) => Gri + 2);
        setLock(true);
      } else if (option === 4) {
        setHuf((Huf) => Huf + 2);
        setRav((Rav) => Rav + 1);
        setLock(true);
      }
    }
  },
  (option, setRav, setSly, setGri, setHuf, lock, setLock) => {
    if (lock === false) {
      if (option === 1) {
        setRav((Rav) => Rav + 1);
        setLock(true);
      } else if (option === 2) {
        setSly((Sly) => Sly + 2);
        setLock(true);
      } else if (option === 3) {
        setGri((Gri) => Gri + 2);
        setLock(true);
      } else if (option === 4) {
        setHuf((Huf) => Huf + 2);
        setRav((Rav) => Rav + 1);
        setLock(true);
      }
    }
  },
  (option, setRav, setSly, setGri, setHuf, lock, setLock) => {
    if (lock === false) {
      if (option === 1) {
        setRav((Rav) => Rav + 1);
        setLock(true);
      } else if (option === 2) {
        setSly((Sly) => Sly + 2);
        setLock(true);
      } else if (option === 3) {
        setGri((Gri) => Gri + 2);
        setLock(true);
      } else if (option === 4) {
        setHuf((Huf) => Huf + 2);
        setRav((Rav) => Rav + 1);
        setLock(true);
      }
    }
  },
  (option, setRav, setSly, setGri, setHuf, lock, setLock) => {
    if (lock === false) {
      if (option === 1) {
        setRav((Rav) => Rav + 1);
        setLock(true);
      } else if (option === 2) {
        setSly((Sly) => Sly + 2);
        setLock(true);
      } else if (option === 3) {
        setGri((Gri) => Gri + 2);
        setLock(true);
      } else if (option === 4) {
        setHuf((Huf) => Huf + 2);
        setRav((Rav) => Rav + 1);
        setLock(true);
      }
    }
  },
  (option, setRav, setSly, setGri, setHuf, lock, setLock) => {
    if (lock === false) {
      if (option === 1) {
        setRav((Rav) => Rav + 1);
        setLock(true);
      } else if (option === 2) {
        setSly((Sly) => Sly + 2);
        setLock(true);
      } else if (option === 3) {
        setGri((Gri) => Gri + 2);
        setLock(true);
      } else if (option === 4) {
        setHuf((Huf) => Huf + 2);
        setRav((Rav) => Rav + 1);
        setLock(true);
      }
    }
  },
  (option, setRav, setSly, setGri, setHuf, lock, setLock) => {
    if (lock === false) {
      if (option === 1) {
        setRav((Rav) => Rav + 1);
        setLock(true);
      } else if (option === 2) {
        setSly((Sly) => Sly + 2);
        setLock(true);
      } else if (option === 3) {
        setGri((Gri) => Gri + 2);
        setLock(true);
      } else if (option === 4) {
        setHuf((Huf) => Huf + 2);
        setRav((Rav) => Rav + 1);
        setLock(true);
      }
    }
  }
];
